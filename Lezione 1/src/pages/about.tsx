import React from 'react';
import Layout from './component/Layout/Layout';

const about = () => {
    return (
        <Layout>
            about project
        </Layout>
    );
};

export default about;