import React from 'react';
import Layout from './component/Layout/Layout';

const contact = () => {
    return (
        <Layout>
            contact project
        </Layout>
    );
};

export default contact;